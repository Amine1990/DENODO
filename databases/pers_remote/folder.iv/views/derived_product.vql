CREATE OR REPLACE VIEW derived_product FOLDER = '/iv' AS SELECT "PRODUCT"."ID" AS "ID", "PRODUCT"."NAME" AS "NAME" FROM "PRODUCT" WHERE "ID" in (1, 2, 3);

ALTER VIEW derived_product
 LAYOUT ("PRODUCT" = [165, 133, 200, 200]);

ALTER VIEW derived_product
 CACHE PARTIAL
     BATCHSIZEINCACHE DEFAULT TIMETOLIVEINCACHE DEFAULT;

